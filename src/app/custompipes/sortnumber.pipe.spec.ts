import { SortnumberPipe } from './sortnumber.pipe';

describe('SortnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SortnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
