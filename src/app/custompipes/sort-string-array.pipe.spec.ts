import { SortStringArrayPipe } from './sort-string-array.pipe';

describe('SortStringArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortStringArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
